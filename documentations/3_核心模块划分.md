# 应用层

+ 要求实现：用户入口，进行参数解析，并调用相应业务。

```
CLI ----->
CommandDispatcher 命令调度 --->
service
```

# 文件核心

+ 实现对象Note来表示一篇笔记。

# 存储层

+ 负责持久化文件，管理文件。

~~~
read(path)
write(path)
delete(path)
~~~

+ 连接sqlite，存储元数据信息。

# 文件解析模块

+ 解析文件，得到文件元数据。

# 查找模块

+ 通过`unordered_map<string, postlist>`存放标签内容。
+ 通过标签快速搜索文件信息。

# 基础设施模块

+ logger
+ config
+ threadpool
+ timer
+ benchmark