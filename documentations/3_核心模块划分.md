# 应用层

+ 要求实现：用户入口，进行参数解析，并调用相应业务。

```
CLI ----->
CommandDispatcher 命令调度 --->
service
```

+ 将应用层分开为CLI Layer和Application Layer，

## CLI Layer

+ 职责：

  1. 接收用户输入

  2. 解析命令

  3. 显示结果

## Application Layer

+ 职责：
  + 业务流程协调

# 文件核心(core)

+ 实现对象Note来表示一篇笔记。

+ 将Note设计为领域模型，将程序中的对象对应为真实业务的概念。

+ 将Note拆分为MetaData和NoteContent两部分。

+ 将MetaData代表用户的一篇笔记，对于：

  ```
  MetaData
  
  id:  唯一身份
  1001
  
  title: 标题
  C++ Memory Management
  
  location:
  notes/cpp_memory.md
  
  created:
  2026-07-31
  
  modified:
  2026-07-31
  
  tags:
  [c++, memory]
  
  content: 当需要时加载到内存
  ```

+ 在MetaData中并没有文本，因为设计的是**延迟加载内容**，所有MetaData只负责描述。
+ 而NoteContent则在用户需要时加载到内存中，这样可以在未来支持缓存功能。

# 存储层

+ 负责持久化文件，管理文件。

~~~
read(path)
write(path)
delete(path)
~~~

+ 连接sqlite，存储元数据信息。

+ SQLite表设计

  ```
  notes 表
  
  notes
  
  id
  
  title
  
  path
  
  created_time
  
  modified_time
  ```

  ```
  tags 表
  
  tags
  
  id
  
  name
  ```

  ```
  note_tags 关系表
  
  note_tags
  
  note_id
  
  tag_id
  
  
  Note
  
  1
  |
  |
  N
  
  note_tags
  
  N
  |
  |
  1
  
  Tag
  ```

  

# 文件解析模块

+ 解析文件，得到文件元数据。

# 查找模块

+ 通过`unordered_map<string, postlist>`存放标签内容。
+ 一种搜索模式为，通过标签快速搜索文件信息，找到文件。
+ 另一种为，搜索文件正文内容。

# 基础设施模块

+ logger
+ config
+ threadpool
+ timer
+ benchmark